# Warm / Warm-lite / Cold 가격 예측 모델 상세 발표자료

- 기준 문서: `partner_warm_cold_best_model_report.html`
- 목적: 각 모델이 어떤 조건에서 선택되고, 어떤 피처와 산식으로 가격을 계산하는지 설명
- 대상: 협력사 설명회, 내부 의사결정자, 모델 운영 담당자
- 권장 시간: 발표 150~180분 + 질의 30분
- 기준 API 표기: 현재 운영 라우팅은 `0.1v API` 기준. 내부 artifact 버전명은 API 버전으로 해석하지 않음

---

## 1. 발표 목표와 진행 방식

**목표**

Warm, Warm-lite, Cold를 성능 숫자만으로 소개하지 않고, 실제 가격 예측이 만들어지는 계산 흐름으로 설명한다.

| 설명할 질문 | 발표에서 답하는 방식 |
|---|---|
| 어떤 작품이 어느 경로로 가는가 | 작가 매칭 신뢰도와 같은 작가 가격 이력 수로 설명 |
| 각 경로는 어떤 피처를 쓰는가 | 모델별 피처 지도와 단계별 사용처로 설명 |
| 최종 가격은 어떻게 계산되는가 | 로그가격 계산식, 후보 선택, 보정, `exp()` 변환으로 설명 |
| 왜 Warm과 Warm-lite를 나누는가 | 1~4건, 5건 이상 반례 실험 근거로 설명 |
| Cold는 왜 구조가 다른가 | 같은 작가 가격 이력이 없는 조건에서 검색 피처와 불확실성 방어가 필요한 이유로 설명 |

**권장 흐름**

공통 라우팅과 용어를 먼저 설명한 뒤, Warm -> Warm-lite -> Cold 순서로 같은 구조를 반복한다.

---

## 2. 현재 0.1v API 라우팅 기준

```text
[작품 입력]
  - 작가명 / artist_key
  - 작품 크기
  - 매체 / 지지체
  - 작가 메타 / 검색 문맥
        |
        v
[라우팅 판단]
  - 작가매칭신뢰도점수 >= 0.80 ?
  - 같은작가_사용가능가격이력수는 몇 건인가?
        |
        +-- 0.80 이상 AND 5건 이상  -> Warm
        |
        +-- 0.80 이상 AND 1~4건     -> Warm-lite
        |
        +-- 그 외                    -> Cold 또는 검수대기
```

| 경로 | 현재 0.1v API 입력 조건 | 가격 판단의 중심 |
|---|---|---|
| Warm | 작가 매칭 신뢰도 0.80 이상 + 같은 작가 이력 5건 이상 | 같은 작가 가격 이력과 정밀 유사작품 비교군 |
| Warm-lite | 작가 매칭 신뢰도 0.80 이상 + 같은 작가 이력 1~4건 | 적은 작가 이력 통계와 작품 기본 피처 |
| Cold | 이력 0건, 작가 매칭 불확실, 입력 부족, 동명이인 위험 | 작품 피처, 작가 메타, 검색 문맥, 불확실성 방어 |

---

## 3. 라우팅 값 1: 작가매칭신뢰도점수

**목적**

입력 작가가 학습 데이터의 특정 작가와 같은 사람인지 0~1 사이 점수로 판단한다.

```text
작가키 직접 일치:
  작가매칭신뢰도점수 = 1.00

작가키가 없거나 신뢰하기 어려운 경우:
  작가매칭신뢰도점수 =
    clip(
        0.70 * 이름일치점수
      + 0.20 * 보조정보일치점수
      + 0.10 * 학습작가등록점수
      - 0.15 * 동명이인위험점수
      - 0.20 * 핵심정보충돌점수,
      0.00,
      0.95
    )
```

| 요소 | 의미 | 예측 경로 영향 |
|---|---|---|
| 이름일치점수 | 정규화 작가명 또는 문자열 유사도 기반 점수 | 높을수록 Warm/Warm-lite 가능성 증가 |
| 보조정보일치점수 | 생년, 국적, 외부 작가 ID 등 일치 여부 | 동명이인 위험을 줄임 |
| 학습작가등록점수 | 학습 데이터에 해당 작가가 존재하는지 | 같은 작가 이력 사용 가능성 확인 |
| 동명이인위험점수 | 같은 이름 후보가 여러 명인지 | 높으면 Cold 또는 검수대기 |
| 핵심정보충돌점수 | 생년/국적/외부 ID 충돌 여부 | 충돌 시 상위 경로 금지 |

---

## 4. 라우팅 값 2: 같은작가_사용가능가격이력수

**이 값은 단순 작가 등장 횟수가 아니다.**

가격 예측에 실제로 사용할 수 있는 같은 작가의 학습 가격 이력 수다.

| 포함 조건 | 제외 조건 |
|---|---|
| 같은 작가로 확정된 학습 데이터 작품 | 가격이 없거나 0 이하인 행 |
| 실제 가격값이 있고 로그가격 계산 가능 | 동일 작품/동일 거래 중복 |
| 예측 대상 작품과 중복되지 않음 | 평가 대상의 정답 가격 |
| 자기 fold 제외 후 남은 학습 이력 | 작가 매칭이 불확실한 후보 |

```text
작가매칭신뢰도점수 >= 0.80 AND 이력 >= 5       -> Warm
작가매칭신뢰도점수 >= 0.80 AND 이력 1~4건      -> Warm-lite
그 외                                          -> Cold 또는 검수대기
```

**주의**

Warm 내부의 `최소 표본 1`은 Warm 진입 조건을 1건으로 낮춘다는 뜻이 아니다. 이미 Warm에 들어온 5건 이상 작가 안에서 더 좁은 하위 유사작품 비교군을 1건부터 인정한다는 뜻이다.

---

## 5. 공통 계산 단위

| 표현 | 의미 | 사용 위치 |
|---|---|---|
| 로그가격 | 가격에 자연로그를 적용한 값 | 세 경로의 내부 계산 단위 |
| `exp()` | 로그가격을 원화 가격으로 되돌리는 함수 | 최종 가격 변환 |
| 잔차 | `실제_로그가격 - 예측_로그가격` | 학습/검증에서 보정 모델 target 또는 평가에 사용 |
| `clip(x, a, b)` | 값을 a~b 범위로 제한 | Warm 잔차 보정, Cold 구간 보정 |
| Quantile | 가격 분포의 특정 위치 | Cold 기준 후보와 방어 후보, Warm 위험도 |
| lookup | 미리 만든 조회표에서 값을 찾는 방식 | Cold 작가 검색 보정 |

```text
최종_원화가격 = exp(최종_로그가격)

APE =
  abs(최종_원화가격 - 실제_원화가격)
  / 실제_원화가격
```

---

## 6. 경로별 성능 기준 요약

| 경로 | 평가 기준 | 평가 행 수(n) | MdAPE | MAPE | p95 APE | 해석 |
|---|---|---:|---:|---:|---:|---|
| Warm 성능 기준 | fixed test | 607 | 0.104326 | 0.235814 | 0.739416 | 5건 이상 이력에서 정밀 비교군과 위험도 방어가 효과적 |
| Warm-lite 성능 기준 | 실존 저이력 LOO x3 | 1,947 | 0.109200 | 0.286600 | 0.876500 | 1~4건 이력은 Cold보다 훨씬 강한 신호 |
| Cold 성능 기준 | fixed test | 3,099 | 0.409820 | 0.849260 | 2.346465 | 같은 작가 이력이 없는 어려운 조건에서 검색 피처와 guard로 방어 |

**n 해석**

`n`은 학습 작품 수가 아니라 성능 지표를 계산한 평가 행 수다.

**RMSE log**

Cold 비교값이 없어 세 경로 비교표에는 넣지 않는다. Warm `0.377190`, Warm-lite `0.447586`은 경로 내부의 보조 검증값이다.

---

## 7. Warm과 Warm-lite를 분리한 실험 근거

| 질문 | 확인 결과 | 근거 |
|---|---|---|
| 1~4건 이력을 Cold로 보내도 되는가 | 안 된다. Warm-lite가 동일 행 Cold보다 크게 우세 | PP-WCUT4: Warm-lite `0.1092 / 0.2866 / 0.8765`, Cold `0.5429 / 0.9946 / 2.5358` |
| 1~4건 전체를 Warm 계열로 통일해도 되는가 | 현재 근거로는 지지되지 않음 | PP-WMIN9C: Warm-lite `0.1092 / 0.2866 / 0.8765`, WMIN8 svc-core proxy `0.1291 / 0.2932 / 0.9163` |
| 5건 이상에 Warm-lite를 강제 적용하면 어떤가 | WMIN8 Warm이 전 지표 우세 | PP-WMIN9D: WMIN8 `0.104326 / 0.235814 / 0.739416`, 강제 Warm-lite `0.108722 / 0.248054 / 0.837824` |

**해석**

1~4건은 Cold로 보내기에는 작가 이력 신호가 강하고, 5건 이상 Warm 본체를 그대로 쓰기에는 저표본 구조가 다르다. 그래서 현재 0.1v API는 `5건 이상 Warm`, `1~4건 Warm-lite`로 분리한다.

---

## 8. Warm 모델 목적

**적용 대상**

작가 매칭 신뢰도가 0.80 이상이고, 같은 작가의 사용 가능한 가격 이력이 5건 이상인 작품.

**핵심 구조**

```text
같은 작가 유사작품 기준가격
  + 보조 안정 Warm 예측값 일부 혼합
  + Huber 잔차 보정
  + 위험도 조건부 후보 선택
  -> 최종 Warm 로그가격
```

| 관점 | Warm의 의미 |
|---|---|
| 주 가격 신호 | 같은 작가의 과거 가격대 |
| 보조 신호 | 큰오차방어 Warm 예측값, 버킷순차보정 Warm 예측값 |
| 보정 방식 | 기준가격 위에 제한된 잔차 보정을 더함 |
| 방어 방식 | 위험도가 높은 작품만 보수 대안 후보로 교체 |

---

## 9. Warm 전체 계산 흐름

```text
[Warm 입력 작품]
  - artist_key
  - 크기 / 면적 / 매체 / 지지체
  - 같은 작가 이력 5건 이상
        |
        v
[같은 작가 유사작품 비교군 기준가격]
  - 작가+재료/지지체+크기
  - 작가+크기
  - 작가 전체
        |
        v
[보조안정 Warm 예측값]
  = 0.75 * 큰오차방어_Warm예측로그가격
    + 0.25 * 버킷순차보정_Warm예측로그가격
        |
        v
[기본/대안 기준가격 생성]
        |
        v
[Huber 잔차 보정]
        |
        v
[위험도 조건부 라우터]
        |
        v
[최종 Warm 로그가격 -> exp() -> 원화 가격]
```

---

## 10. Warm 피처 지도

| 단계 | 사용하는 피처 | 역할 |
|---|---|---|
| 라우팅 | `작가매칭신뢰도점수`, `같은작가_사용가능가격이력수` | Warm 적용 가능 여부 판단 |
| 유사작품 비교군 | `artist_key`, `medium_category`, `support_category`, `size_bucket` | 같은 작가 안에서 가장 가까운 비교군 선택 |
| 비교군 통계 | `같은작가_유사작품기준로그가격`, `비교군표본수로그`, `비교군가격IQR` | 기준가격과 신뢰도 피처 생성 |
| 보조 안정 | 큰오차방어 Warm 예측값, 버킷순차보정 Warm 예측값 | 비교군 기준가격이 흔들릴 때 완충 |
| Huber 잔차 | 보조안정값, 수축 prior, `log_area`, 비교군 표본 수, 후보 간 차이 | 기준가격에 남은 반복 오차를 제한적으로 보정 |
| 위험도 라우터 | Quantile예측구간폭, 후보예측분산, 기준-안정차이, 신뢰도구간, 가격대 | 기본 후보와 대안 후보 중 최종 후보 선택 |

---

## 11. Warm 유사작품 비교군 기준가격

**비교군 매칭 사다리**

| 우선순위 | 비교군 그룹 | 최소 표본 | 의미 |
|---:|---|---:|---|
| 1 | 작가 + 재료/지지체 + 크기 구간 | 1 | 같은 작가의 가장 가까운 시장 조건 |
| 2 | 작가 + 크기 구간 | 1 | 매체/지지체가 부족할 때 크기 중심 |
| 3 | 작가 전체 이력 | 1 | 같은 작가 가격대 유지 |
| 4 | 재료/지지체 + 크기, 작가 무관 | 30 | 작가 비교군이 비면 쓰는 일반 비교군 |
| 5~7 | 재료/크기 일반 그룹, 전체 학습 데이터 | 30~50 / 제한 없음 | 최종 fallback |

```text
같은작가_유사작품기준로그가격
  = 선택된 비교군의 로그가격 통계 기반 기준가격
```

**중요**

자기 fold를 제외한 뒤 비교군이 0건이면 다음 단계로 fallback한다. 평가 대상 작품의 정답 가격이 자기 피처에 들어가지 않게 하기 위한 누수 차단이다.

---

## 12. Warm 보조안정 예측값

`보조안정_Warm예측로그가격`은 단순 평균이나 임시값이 아니라, 이미 학습·동결된 두 예측 축을 로그가격에서 섞은 보조 기준이다.

```text
보조안정_Warm예측로그가격
  = 0.75 * 큰오차방어_Warm예측로그가격
    + 0.25 * 버킷순차보정_Warm예측로그가격
```

| 축 | 생성 방식 | Warm에서의 역할 |
|---|---|---|
| 큰오차방어 Warm 예측값 | 여러 Warm 후보 예측값, 후보 간 평균/분산/범위, 불확실성 폭을 입력으로 한 Huber meta-model | 큰 오차 가능성이 있는 작품에서 안정적인 보조 기준 제공 |
| 버킷순차보정 Warm 예측값 | 생성 bucket -> CatBoost Quantile -> Huber 중심선 -> CatBoost 잔차 보정 | 작품 크기/형태/매체 조건을 반영한 보조 예측 축 |

**해석**

Warm은 같은 작가 유사작품 기준가격을 중심으로 보되, 그 값이 지나치게 흔들리지 않도록 보조안정 예측값을 일부 섞는다.

---

## 13. 버킷순차보정 Warm 예측값 생성

```text
[작품 기본 피처]
  - width_cm, height_cm, depth_cm
  - area_cm2, log_area, aspect_ratio
  - medium_category, support_category, artist_key
        |
        v
[생성 bucket 피처]
  - size_bucket, shape_bucket
  - support_size_bucket, medium_shape_bucket
  - is_large_2d, is_large_3d
        |
        v
[CatBoost Quantile]
  - q10_log, q50_log, q90_log
  - quantile_width = q90_log - q10_log
        |
        v
[Huber 중심선]
  - 안정적인 중심 로그가격 계산
        |
        v
[CatBoost 잔차 보정]
  - 중심선에 남은 오차를 예측
        |
        v
[버킷순차보정_Warm예측로그가격]
```

**주의**

이 값은 고정 산식 하나가 아니라 학습된 모델 파이프라인의 출력이다. 예측 시점에는 새로 학습하지 않고, 동결된 bucket 규칙과 모델을 적용한다.

---

## 14. 버킷순차보정의 학습 단계와 예측 단계

| 구분 | 실제 가격 사용 여부 | 처리 |
|---|---|---|
| 학습 단계 | 사용함 | 실제 로그가격으로 Quantile/Huber/CatBoost 잔차 모델을 학습 |
| OOF 잔차 생성 | 사용함 | `실제_로그가격 - Huber_OOF_중심선_로그가격`을 CatBoost 잔차 target으로 생성 |
| 운영 예측 단계 | 사용하지 않음 | 새 작품 피처만으로 q10/q50/q90, Huber 중심선, 잔차 보정값을 예측 |

```text
학습 때:
  잔차 target = 실제_로그가격 - Huber_OOF_중심선_로그가격

예측 때:
  버킷순차보정_Warm예측로그가격
    = 동결 Huber 중심선 로그가격
      + 동결 CatBoost 잔차모델 예측값
```

**해석**

Huber 중심선은 먼저 안정적인 중심 가격을 잡는 기준선이고, CatBoost 잔차 모델은 그 뒤에 반복적으로 남는 오차만 보정한다.

---

## 15. Warm 기준가격 후보 생성

```text
기본후보_기준가 =
  0.70 * 같은작가_유사작품기준로그가격
  + 0.30 * 보조안정_Warm예측로그가격

대안후보_기준가 =
  0.85 * 같은작가_유사작품기준로그가격
  + 0.15 * 보조안정_Warm예측로그가격
```

| 후보 | 의미 | 언제 중요해지는가 |
|---|---|---|
| 기본 후보 | 일반적으로 유지하는 기준가격 | 대부분의 Warm 작품 |
| 대안 후보 | 같은 작가 유사작품 신호를 더 강하게 반영한 후보 | 위험도 0.2534 이상이고 대안 후보가 기본 후보보다 0.005 log 이상 낮을 때 |

**논리**

기준가격의 중심은 같은 작가 유사작품이다. 다만 비교군이 작거나 가격 분산이 클 수 있으므로 보조안정 값을 일부 섞어 기준가격의 급격한 흔들림을 줄인다.

---

## 16. Warm Huber 잔차 보정

```text
잔차예측 = Huber잔차모델(10개 피처)

후보_보정로그가격 =
  후보_기준가 + clip(0.50 * 잔차예측, -0.05, +0.05)
```

| 장치 | 의미 | 이유 |
|---|---|---|
| Huber loss | 큰 오차 작품의 영향을 완만하게 처리하는 강건 회귀 | 이상치가 회귀계수를 과하게 흔드는 것을 줄임 |
| 0.50 배율 | 잔차 예측값의 절반만 반영 | 보정 모델이 기준가격을 뒤집지 못하게 함 |
| `clip(-0.05, +0.05)` | 보정 폭을 로그가격 ±0.05로 제한 | Warm의 주 신호인 같은 작가 기준가격을 보호 |

**Huber는 학습된다**

Huber 자체는 손실 함수지만, 여기서의 Huber 잔차 모델은 Huber loss를 기준으로 회귀계수와 절편을 적합한 예측기다.

---

## 17. Warm 위험도 조건부 라우터

```text
작품단위위험도 =
  clip(
      0.38 * clip((Quantile예측구간폭 - 1.20) / 0.95, 0, 1)
    + 0.22 * clip(후보예측분산 / 0.18, 0, 1)
    + 0.14 * clip(현재-안정기준차이 / 0.06, 0, 1)
    + 0.16 * (신뢰도구간 == 저신뢰)
    + 0.10 * (안정기준가격대 == 초고가),
      0,
      1
    )

위험도임계 = 0.2534

대안교체조건 =
  작품단위위험도 >= 0.2534
  AND 기본후보_보정로그가격 - 대안후보_보정로그가격 >= 0.005
```

**결정**

대안교체조건이 참이면 대안 후보를 쓰고, 아니면 기본 후보를 유지한다.

---

## 18. Warm 계산 예시 1: 라우팅과 비교군

```text
입력:
  작가 매칭 신뢰도 = 0.92
  같은 작가 사용 가능 이력 = 12건
  작품 = oil/canvas, 100cm x 80cm
  log_area = log(8000) = 8.987

라우팅:
  0.92 >= 0.80
  12건 >= 5건
  -> Warm 경로 진입

비교군:
  1순위: 같은 작가 + oil/canvas + 비슷한 크기
  자기 fold 제외 후 남은 작품 = 3건
  로그가격 = [18.100, 18.300, 18.500]

같은작가_유사작품기준로그가격 = median(...) = 18.300
비교군표본수로그 = log1p(3) = 1.386
비교군가격IQR = 0.200
```

---

## 19. Warm 계산 예시 2: 보조안정과 후보

```text
큰오차방어_Warm예측로그가격 = 18.500

버킷순차보정 내부:
  CatBoost Quantile:
    q10_log = 17.700
    q50_log = 18.250
    q90_log = 18.900
    quantile_width = 1.200

  Huber 중심선 로그가격 = 18.260
  CatBoost 잔차보정 = +0.040

버킷순차보정_Warm예측로그가격 = 18.260 + 0.040 = 18.300

보조안정_Warm예측로그가격 =
  0.75 * 18.500 + 0.25 * 18.300 = 18.450

기본후보_기준가 =
  0.70 * 18.300 + 0.30 * 18.450 = 18.345

대안후보_기준가 =
  0.85 * 18.300 + 0.15 * 18.450 = 18.323
```

---

## 20. Warm 계산 예시 3: 잔차, 위험도, 최종

```text
Huber 잔차 보정:
  기본후보 잔차예측 = +0.020
  기본후보 보정값 = clip(0.50 * 0.020, -0.05, +0.05) = +0.010
  기본후보_보정로그가격 = 18.355

  대안후보 잔차예측 = +0.016
  대안후보 보정값 = clip(0.50 * 0.016, -0.05, +0.05) = +0.008
  대안후보_보정로그가격 = 18.331

위험도:
  Quantile예측구간폭 = 1.65
  후보예측분산 = 0.08
  현재-안정기준차이 = 0.03
  작품단위위험도 = 0.348

라우터:
  0.348 >= 0.2534
  18.355 - 18.331 = 0.024 >= 0.005
  -> 대안 후보 선택

최종_Warm_로그가격 = 18.331
최종_Warm_가격_KRW = exp(18.331) ~= 91,000,000원
```

---

## 21. Warm 성능과 해석

| 평가 | n | MdAPE | MAPE | p95 APE | RMSE log |
|---|---:|---:|---:|---:|---:|
| Warm fixed test | 607 | 0.104326 | 0.235814 | 0.739416 | 0.377190 |
| Warm validation OOF | 519 | 0.094033 | 0.175114 | 0.571291 | - |

| 검증 | 결과 |
|---|---|
| 이전 Warm 기준선 대비 | MdAPE -26%, MAPE -13%, p95 -8% |
| API parity | 607/607건 성공 |
| 최대 로그가격 차이 | `5.33e-15` |

**해석**

Warm은 같은 작가 유사작품 기준가격을 크게 흔들지 않고, 작은 잔차 보정과 위험도 조건부 방어로 큰 오차를 줄이는 경로다.

---

## 22. Warm-lite 모델 목적

**적용 대상**

작가 매칭 신뢰도가 0.80 이상이고, 같은 작가의 사용 가능한 가격 이력이 1~4건인 작품.

**핵심 구조**

```text
같은 작가 1~4건 이력 통계
  + 작품 크기/매체/지지체 피처
  + 이력 매칭 단계 피처
  -> 6개 Huber 회귀 예측값 평균
  -> 최종 Warm-lite 로그가격
```

| Warm-lite의 역할 | 설명 |
|---|---|
| Cold 대체 | 이력 1건이라도 있으면 Cold보다 강한 작가 실제 가격 신호를 활용 |
| Warm 대체가 아님 | 5건 이상 Warm 본체의 위험도 라우터를 저표본에 그대로 쓰지 않음 |
| 저표본 관리 | 이력 1건은 `warm_lite_low`와 검수 플래그로 관리 |

---

## 23. Warm-lite 계산 흐름

```text
[Warm-lite 입력 작품]
  - 작가 매칭 신뢰도 0.80 이상
  - 같은 작가 이력 1~4건
  - 작품 크기 / 매체 / 지지체
        |
        v
[작가 이력 통계 생성]
  - 같은 작가 + 같은 매체/지지체 + 같은 크기
  - 같은 작가 + 같은 크기
  - 같은 작가 전체 이력
        |
        v
[Warm-lite 피처 구성]
  - 작품 크기/형태 피처
  - 작품 범주 피처
  - 작가 로그가격 통계
  - 작가 면적단가 통계
  - 표본 수와 매칭 단계
        |
        v
[Huber 6구성 ensemble]
        |
        v
[등급/검수 플래그]
        |
        v
[최종 Warm-lite 로그가격 -> exp() -> 원화 가격]
```

---

## 24. Warm-lite 피처 지도

| 피처 묶음 | 예시 | 역할 |
|---|---|---|
| 작품 크기/형태 | `width_cm`, `height_cm`, `depth_cm`, `area_cm2`, `log_area`, `aspect_ratio`, `has_depth`, `is_3d_candidate` | 작품 규모와 형태에 따른 가격 차이 반영 |
| 작품 범주 | `medium_category`, `support_category`, `size_bucket`, `medium_support_bucket` | 매체/지지체/크기 조합의 가격 패턴 반영 |
| 작가 이력 수 | `artist_history_count`, `grp_n_log` | 1건과 2~4건의 신뢰도 차이 반영 |
| 작가 로그가격 통계 | `grp_log_price_median`, `grp_log_price_q25`, `grp_log_price_q75`, `grp_log_price_iqr` | 저이력 작가의 직접 가격 수준과 분산 반영 |
| 작가 면적단가 통계 | `grp_unit_area_median`, `grp_unit_area_iqr`, `grp_price_proxy` | 대상 작품 크기에 맞게 작가 가격 수준 보정 |
| 이력 매칭 단계 | `grp_match_level` | 가장 가까운 이력을 썼는지, 작가 전체 이력을 썼는지 구분 |
| fallback 통계 | 동결된 비작가 비교군 통계 | 정상 1~4건 입력에서는 예외 방어용 |

---

## 25. Warm-lite 작가 이력 매칭 사다리

```text
1순위: 같은 작가 + 같은 매체/지지체 + 같은 크기 구간
2순위: 같은 작가 + 같은 크기 구간
3순위: 같은 작가 전체 1~4건 이력
4순위: 동결된 비작가 비교군 통계
```

| 단계 | 의미 |
|---|---|
| 1순위 | 대상 작품과 가장 가까운 같은 작가 이력 |
| 2순위 | 매체/지지체 표본이 부족할 때 크기 조건 중심 |
| 3순위 | 세부 조건은 약하지만 작가 가격대 자체는 유지 |
| 4순위 | 통계 생성 실패나 입력 결측에 대비한 방어 |

**주의**

Warm-lite는 같은 작가 이력 1~4건이 있어야 들어오는 경로다. 4순위 fallback은 작가 이력 없이 Warm-lite를 쓰겠다는 뜻이 아니다.

---

## 26. Warm-lite Huber 6구성 ensemble

```text
최종_Warm-lite_로그가격 =
  mean(Huber_1, Huber_2, Huber_3, Huber_4, Huber_5, Huber_6)
```

| 구성 | 입력 피처 | 역할 |
|---|---|---|
| Huber 1~4 | 작품 크기/형태 8개 + 로그가격 median/q25/q75/IQR + 면적단가 median/IQR + `grp_price_proxy` + 표본 수/매칭 단계 + 범주 피처 | 작가 가격 분포 정보를 넓게 사용 |
| Huber 5~6 | 작품 크기/형태 8개 + 로그가격 median/IQR + `grp_price_proxy` + 표본 수/매칭 단계 + 범주 피처 | 저표본 분위수 흔들림을 줄인 보수적 구성 |

**Huber 의미**

Huber loss 기준으로 회귀계수와 절편을 학습한 예측기다. 예측 시점에는 새 작품 피처만 넣어 로그가격을 계산한다.

---

## 27. Warm-lite 계산 예시 1: 입력과 작가 통계

```text
라우팅:
  작가 매칭 신뢰도 = 0.87
  같은 작가 사용 가능 이력 k = 2건
  0.87 >= 0.80 AND 1 <= k <= 4
  -> Warm-lite 경로

입력 작품:
  acrylic/paper, 72cm x 60cm
  area_cm2 = 4,320
  log_area = 8.371
  aspect_ratio = 1.200
  size_bucket = 중형 예시

이력 매칭:
  1순위는 부족
  2순위 같은 작가 + 같은 크기 구간에서 2건 사용

이력 로그가격:
  [17.450, 17.600]
  작가로그가격중앙값 = 17.525
  q25 = 17.488
  q75 = 17.563
  IQR = 0.075
```

---

## 28. Warm-lite 계산 예시 2: 면적보정과 ensemble

```text
작가 면적단가:
  이력 1: 17.450 - 8.200 = 9.250
  이력 2: 17.600 - 8.550 = 9.050
  작가면적단가로그중앙값 = 9.150

면적보정작가기준로그가격 =
  9.150 + 대상작품_log_area 8.371 = 17.521

Huber 예측:
  Huber_1 = 17.490
  Huber_2 = 17.530
  Huber_3 = 17.510
  Huber_4 = 17.550
  Huber_5 = 17.500
  Huber_6 = 17.520

최종_Warm-lite_로그가격 =
  (17.490 + 17.530 + 17.510 + 17.550 + 17.500 + 17.520) / 6
  = 17.517

k = 2 -> 표준 Warm-lite 등급
최종_Warm-lite_가격_KRW = exp(17.517) ~= 40,500,000원
```

---

## 29. Warm-lite 성능과 유지 근거

| 평가 | n | MdAPE | MAPE | p95 APE |
|---|---:|---:|---:|---:|
| Warm-lite | 1,947 | 0.1092 | 0.2866 | 0.8765 |
| 동일 행 Cold | 1,947 | 0.5429 | 0.9946 | 2.5358 |

```text
MdAPE 기준:
  Cold / Warm-lite = 0.5429 / 0.1092 = 4.97배
```

| 이력 수 | Warm-lite | WMIN8 svc-core proxy | 판정 |
|---:|---:|---:|---|
| 1 | `0.1207 / 0.3415 / 0.9559` | `0.1271 / 0.3406 / 0.9573` | 혼합, Warm-lite MdAPE/p95 우세 |
| 2 | `0.1184 / 0.2707 / 0.8779` | `0.1448 / 0.2821 / 0.9478` | Warm-lite 전 지표 우세 |
| 3 | `0.1060 / 0.2541 / 0.7142` | `0.1195 / 0.2661 / 0.7489` | Warm-lite 전 지표 우세 |
| 4 | `0.0923 / 0.2557 / 0.7884` | `0.1190 / 0.2634 / 0.7682` | 혼합, Warm-lite MdAPE/MAPE 우세 |

---

## 30. Cold 모델 목적

**적용 대상**

같은 작가 가격 이력이 0건이거나, 작가 매칭이 불확실하거나, 동명이인 위험 때문에 같은 작가 이력을 쓰기 어려운 작품.

**핵심 구조**

```text
작품 피처 + 작가 메타 + 검색 피처
  -> LightGBM Quantile 후보
  -> Quantile 폭 구간별 대표 로그가격 안정화
  -> 과대예측 방어 guard
  -> 작가 검색 보정 lookup
  -> 최종 Cold 로그가격
```

| Warm/Warm-lite와 다른 점 | Cold의 대응 |
|---|---|
| 같은 작가 가격 이력이 없음 | 작품/작가/검색 문맥으로 간접 추정 |
| 불확실성이 큼 | Quantile예측구간폭으로 불확실성 측정 |
| 과대예측 위험이 큼 | 낮은쪽 40% 기준과 50:50 guard |
| 검색 문맥이 중요함 | 작가별 검색 보정 lookup 적용 |

---

## 31. Cold 계산 흐름

```text
[Cold 입력 작품]
  - 작품 크기 / 매체 / 지지체
  - 작가 메타
  - 작가명 검색 스냅샷
        |
        v
[피처 생성]
  - 작품 파생 피처
  - 작가 메타 파생 피처
  - 검색 문맥 피처
  - 검색 상호작용 피처
        |
        v
[LightGBM Quantile]
  - 검색피처포함_기초Quantile후보_로그가격
  - 낮은쪽40퍼센트기준_로그가격
  - Quantile예측구간폭
        |
        v
[대표 로그가격 안정화]
        |
        v
[과대예측 방어]
        |
        v
[작가 검색 보정 lookup]
        |
        v
[최종 Cold 로그가격 -> exp() -> 원화 가격]
```

---

## 32. Cold 피처 지도

| 피처 묶음 | 예시 | 역할 |
|---|---|---|
| 작품 크기/형태 | `width_cm`, `height_cm`, `depth_cm`, `area_cm2`, `log_area`, `aspect_ratio` | 작품 규모와 형태의 가격 효과 |
| 작품 범주 | `medium_category`, `support_category`, `medium_support_bucket`, `size_bucket`, `shape_bucket` | 매체/지지체/크기 조합 효과 |
| 작가 메타 | birth year, nationality, career stage, total works, for sale works, followers | 작가 세대/활동량/인지도 신호 |
| 메타 결측 | `artist_meta_*_missing` | 정보 부족 자체의 불확실성 반영 |
| 검색량/출처 | `search_result_count`, `search_source_count`, `search_result_count_log` | 검색 노출과 출처 다양성 |
| 검색 문맥 | art, exhibition, gallery, market, homonym context | 미술/전시/시장 관련성 및 동명이인 위험 |
| 검색 상호작용 | `search_quality_x_log_area`, `search_art_match_x_followers_log` | 검색 신호와 작품/작가 조건 결합 |
| Quantile 산출값 | `y18_qwidth_pred_log`, `lgb_q40_pred_log`, `quantile_width_log` | 기준 후보, 방어 후보, 불확실성 폭 |

---

## 33. Cold 검색 품질 점수

```text
검색품질점수 =
  0.30 * 미술문맥비율
  + 0.20 * 신뢰도메인비율
  + 0.15 * 전시문맥비율
  + 0.15 * 시장/거래문맥비율
  + 0.10 * 최근결과비율
  + 0.10 * 검색제공자커버리지점수
  + 0.10 * 작가명일치비율
  - 0.30 * 동명이인위험비율
```

| 항목 | 의미 |
|---|---|
| 미술/전시/갤러리/시장 문맥 | 검색 결과가 실제 미술 작가 활동과 관련 있는지 |
| 신뢰 도메인 | 갤러리, 미술관, 경매사 등 신뢰 가능한 출처인지 |
| 최근 결과 | 검색 정보가 오래된 흔적만 있는지 보완 |
| 작가명 일치 | 검색 결과가 입력 작가명과 실제로 맞는지 |
| 동명이인 위험 | 같은 이름의 다른 사람 검색 결과가 섞이는 위험 |

**해석**

검색 결과가 많다는 사실보다, 그 검색 결과가 미술 작가 가격 문맥에 맞는지가 중요하다.

---

## 34. Cold LightGBM Quantile 산출값

| 문서용 이름 | 내부 의미 | 역할 |
|---|---|---|
| `검색피처포함_기초Quantile후보_로그가격` | 작품/작가/검색 피처를 넣은 LightGBM Quantile 기준 후보 | Cold 대표 로그가격의 출발점 |
| `낮은쪽40퍼센트기준_로그가격` | 가격 분포의 낮은쪽 40% 위치 예측값 | 과대예측 방어 기준 |
| `Quantile예측구간폭` | 높은 Quantile과 낮은 Quantile의 차이 | 불확실성 측정, 구간 보정, guard 조건 |

```text
작품 피처 + 작가 메타 + 검색 피처 + 검색 상호작용
        |
        v
LightGBM Quantile 회귀
        |
        +--> 기초 Quantile 후보
        +--> 낮은쪽 40% 기준
        +--> Quantile예측구간폭
```

**주의**

`낮은쪽 40% 지점 가격`은 가격을 40% 할인한다는 뜻이 아니다. 조건이 비슷한 작품 가격 분포에서 낮은 순서로 40% 위치를 예측한 값이다.

---

## 35. Cold 대표 로그가격 안정화

```text
Quantile예측구간폭_구간 = bucket(Quantile예측구간폭)

구간별_잔차중앙값 =
  median(
    실제_로그가격 - 검색피처포함_기초Quantile후보_로그가격
  )
  within same Quantile예측구간폭_구간

구간별_보정값 =
  clip(구간별_잔차중앙값, -0.25, +0.25)

검색포함_대표로그가격 =
  검색피처포함_기초Quantile후보_로그가격
  + 구간별_보정값
```

**운영 예측에서의 실제 가격 사용 여부**

구간별 잔차중앙값은 학습/검증 단계에서 미리 계산해 동결한다. 운영 예측 시점에는 실제 가격을 새로 쓰지 않고, Quantile 폭 구간에 맞는 동결 보정값만 조회한다.

---

## 36. Cold 과대예측 방어 guard

```text
방어조건 =
  Quantile예측구간폭 >= 1.4612207078910142
  AND 검색포함_대표로그가격 - 낮은쪽40퍼센트기준_로그가격 >= 0.07715547281151025
  AND 낮은쪽40퍼센트기준_로그가격 < 검색포함_대표로그가격

if 방어조건:
  과대예측방어_로그가격 =
    0.50 * 검색포함_대표로그가격
    + 0.50 * 낮은쪽40퍼센트기준_로그가격
else:
  과대예측방어_로그가격 = 검색포함_대표로그가격
```

**논리**

Cold의 주요 운영 리스크는 신규/무이력 작가의 큰 과대예측이다. 그래서 불확실성이 크고 대표가격이 낮은쪽 40% 기준보다 충분히 높을 때만 하향 방어한다.

---

## 37. Cold 작가 검색 보정 lookup

```text
작가검색보정_로그값 =
  search_delta_lookup[artist_key]

최종_Cold_로그가격 =
  과대예측방어_로그가격
  + clip(작가검색보정_로그값, -0.20, +0.20)
```

| 경우 | 처리 |
|---|---|
| lookup에 작가가 있음 | 작가별 검색 보정값을 최종 로그가격에 더함 |
| lookup에 작가가 없음 | 보정값 0, guard only |
| Quantile 폭이 넓음 | 검수 플래그 또는 낮은 신뢰도 표시 |
| lookup 없음 + 폭 넓음 | 검색 보정 근거 부족과 높은 불확실성을 함께 표시 |

**lookup 의미**

실시간 검색이나 즉석 학습이 아니다. 검증된 검색 스냅샷에서 만든 고정 작가별 검색 보정값 조회표다.

---

## 38. Cold 계산 예시 1: 입력과 Quantile 후보

```text
라우팅:
  작가 매칭 신뢰도 = 0.58
  같은 작가 사용 가능 이력 k = 0건
  0.58 < 0.80, k = 0
  -> Cold 경로

입력 작품:
  mixed media, 65cm x 53cm
  area_cm2 = 3,445
  log_area = 8.145
  aspect_ratio = 1.226
  medium_category = mixed_media
  support_category = unknown

검색 스냅샷:
  검색 결과 수 = 12
  미술 문맥 결과 수 = 8
  갤러리/미술관 문맥 결과 수 = 3
  시장/판매 문맥 결과 수 = 2
  동명이인 위험 문맥 수 = 1

검색 파생:
  search_result_count_log = log1p(12) = 2.565
  search_art_match_ratio = 8 / 12 = 0.667
  search_gallery_ratio = 3 / 12 = 0.250
  search_market_ratio = 2 / 12 = 0.167
  search_quality_score = 0.620

LightGBM Quantile:
  기초Quantile후보 = 17.800
  낮은쪽40퍼센트기준 = 17.550
  Quantile예측구간폭 = 1.700
```

---

## 39. Cold 계산 예시 2: 안정화, guard, lookup

```text
대표 로그가격 안정화:
  Quantile예측구간폭 1.700 구간의 검증 잔차 중앙값 = +0.040
  구간별_보정값 = clip(+0.040, -0.25, +0.25) = +0.040
  검색포함_대표로그가격 = 17.800 + 0.040 = 17.840

guard 조건:
  1.700 >= 1.4612207078910142
  17.840 - 17.550 = 0.290 >= 0.07715547281151025
  17.550 < 17.840
  -> guard 활성화

과대예측방어_로그가격 =
  0.50 * 17.840 + 0.50 * 17.550 = 17.695

작가 검색 보정:
  search_delta_lookup[정규화작가명] = +0.060
  작가검색보정_로그값 = +0.060

최종_Cold_로그가격 = 17.695 + 0.060 = 17.755
최종_Cold_가격_KRW = exp(17.755) ~= 51,400,000원
```

---

## 40. Cold 성능과 해석

| 단계 | MdAPE | MAPE | p95 APE | 해석 |
|---|---:|---:|---:|---|
| 검색포함 대표 로그가격 | 0.424663 | 0.991042 | 3.305298 | 검색 피처 포함 기준 예측 |
| 과대예측 방어 로그가격 | 0.417765 | 0.963963 | 2.537708 | 큰 오차 구간 방어 |
| 검색 보정만 추가한 로그가격 | 0.412921 | 0.875749 | 2.937449 | 평균 오차 개선 |
| 과대예측 방어 + 검색 보정 | 0.409820 | 0.849260 | 2.346465 | 평균 오차와 큰 오차를 동시에 낮춤 |

**해석**

과대예측 방어는 p95를 낮추고, 검색 보정은 평균 오차를 낮춘다. 둘을 함께 적용한 값이 Cold 성능 기준이다.

---

## 41. 세 경로 계산 구조 비교

| 항목 | Warm | Warm-lite | Cold |
|---|---|---|---|
| 적용 조건 | 고신뢰 + 이력 5건 이상 | 고신뢰 + 이력 1~4건 | 이력 0건 또는 매칭 불확실 |
| 주 가격 신호 | 같은 작가 유사작품 비교군 | 같은 작가 저이력 통계 | 작품/작가/검색 문맥 |
| 계산 방식 | 기준가격 + Huber 잔차 + 위험도 라우터 | 6구성 Huber ensemble | LightGBM Quantile + guard + search lookup |
| 불확실성 대응 | 위험 작품만 대안 후보 | 1건은 낮은 등급/검수 | Quantile 폭과 낮은쪽 40% guard |
| 검색 피처 | 직접 사용하지 않음 | 사용하지 않음 | 핵심 입력 및 최종 보정 |
| 최종 변환 | `exp(최종_Warm_로그가격)` | `exp(최종_Warm-lite_로그가격)` | `exp(최종_Cold_로그가격)` |

**핵심**

세 경로는 하나의 모델 옵션이 아니라, 입력 정보 조건이 다른 세 문제를 각각 푸는 구조다.

---

## 42. 학습 단계와 운영 예측 단계 구분

| 항목 | 학습/검증 단계 | 운영 예측 단계 |
|---|---|---|
| 실제 가격 | 모델 학습, 잔차 target, 성능 평가에 사용 | 사용하지 않음 |
| Warm 유사작품 통계 | 자기 fold 제외로 누수 차단 | 확정된 학습 이력에서 통계 조회 |
| Warm 버킷순차보정 | 실제 가격으로 Quantile/Huber/CatBoost 잔차 모델 학습 | 동결 모델로 예측만 수행 |
| Warm-lite 이력 통계 | leave-one-out 또는 fold 제외 | 확정 같은 작가 1~4건 이력으로 통계 생성 |
| Cold 구간별 잔차 | validation에서 미리 계산 후 동결 | Quantile 폭 구간별 동결 보정값만 조회 |
| Cold 검색 lookup | 검증된 검색 스냅샷에서 보정값 생성 | 고정 조회표에서 보정값 조회 |

**핵심**

운영 예측에서 실제 가격 label은 입력으로 쓰지 않는다. 실제 가격은 학습과 검증, 그리고 성능 지표 계산에만 사용된다.

---

## 43. 재현 가능성과 검증 근거

| 구분 | 확인 파일 또는 실험 | 결과 |
|---|---|---|
| Warm WMIN8 성능 | `warm_model_policy_wmin8.json` | fixed test 607건, `0.104326 / 0.235814 / 0.739416` |
| Warm API parity | PP-WMIN10 | 607/607건 성공, max diff `5.33e-15` |
| Warm 최소 표본 1 매칭률 | PP-WMIN2 | 정밀 비교군 매칭률 81.9% |
| Warm-lite 실존 저이력 검증 | PP-WCUT4 | 1,947행, `0.1092 / 0.2866 / 0.8765` |
| Warm-lite vs Warm proxy | PP-WMIN9C / PP-WMIN9E | 1~4 전체 Warm-lite 전 지표 우세 |
| 5건 이상 강제 Warm-lite | PP-WMIN9D | WMIN8 전 지표 우세 |
| Cold 재현 | v0.3 reproduction check | `all_passed=true`, 최대 지표 차이 `1.110e-16` |
| 0.1v 라우팅 상수 | `official_v0_1_service.py` | 0.80, 5건 이상 Warm, 1~4건 Warm-lite |

---

## 44. 발표 결론

**한 문장 요약**

현재 0.1v 가격 예측은 작가 매칭 신뢰도와 같은 작가 가격 이력 수를 기준으로 Warm, Warm-lite, Cold를 나누고, 각 경로는 서로 다른 정보 조건에 맞는 피처와 방어 로직으로 최종 로그가격을 만든다.

| 결론 | 설명 |
|---|---|
| Warm | 5건 이상 같은 작가 이력을 중심으로 정밀 유사작품 기준가격을 만들고, 잔차 보정과 위험도 라우터로 방어 |
| Warm-lite | 1~4건 작가 이력을 Cold로 버리지 않고, 저이력 통계와 작품 피처를 Huber ensemble로 결합 |
| Cold | 같은 작가 이력이 없거나 불확실하므로 작품/작가/검색 피처와 Quantile 기반 과대예측 방어를 사용 |

**마무리 메시지**

이 모델의 핵심은 단순히 어떤 알고리즘을 썼는지가 아니라, 입력 작품이 가진 근거의 양과 신뢰도에 맞춰 계산 경로를 다르게 설계했다는 점이다.
