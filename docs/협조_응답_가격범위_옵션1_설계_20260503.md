# 가격 범위 옵션 1 적용 설계 (additive + 점진 rollout)

> **작성일**: 2026-05-03
> **결정**: 협력자 응답 — **옵션 1 채택** (A/B 범위, C 단일가+문구, D 미노출)
> **출처**: `docs/model_technical_report_v2.html` §6.2
> **코덱스 review**: backward compat + 점진 rollout 권고 반영
> **소요**: backend 약 4d (offline replay 포함) + 점진 rollout (1주~)

## 0. 결정 사항

| 등급 | n | MdAPE | 현재 m | 권장 m | 옵션 1 표시 정책 |
|---|---:|---:|---:|---:|---|
| A | 27,062 | 9.8% | 0.20 | **0.286** | 가격 범위 표시 (새 m 적용) |
| B | 1,006 | 29.7% | 0.30 | **0.609** | 가격 범위 표시 (새 m 적용) |
| C | 128 | 39.0% | 0.50 | (미적용) | 단일 참고가 + "정확도 낮음" 문구 |
| D | 180 | 43.6% | 0.70 | (미적용) | 범위 미노출 (frontend hide) |

## 1. Schema 계약 — additive only (코덱스 P1 fix)

**원칙**: `prediction.price_range` 는 required 유지 (기존 client backward compat). 신규 필드만 추가.

```python
# src/visionai/price_engine/api/primary_schemas.py

class Prediction(BaseModel):
    # 기존 — required 유지
    price_krw: int
    price_usd: float
    price_range: PriceRange  # 모든 등급에 값 채움 (D 도 일단 채움 — 호환)
    confidence_grade: str
    margin: float

    # v3.7 신규 (additive — 기존 client 무시 가능)
    range_display_mode: Literal["range", "single", "hidden"] = "range"
    range_visible: bool = True       # frontend hint (D 면 false)
    disclaimer: str | None = None    # C 면 "정확도 낮음" 문구
    margin_policy_version: str = "v1"  # 'v1' = 옛 m, 'v2' = 새 m
```

**Backward compat 분석**:

| Client | 기존 동작 | 옵션 1 적용 후 |
|---|---|---|
| 외부 strict DTO parser | `price_range` required → 정상 | 정상 (값 채워짐) |
| 신규 첫 party frontend | `price_range` 사용 | `range_visible=false` 면 hide / `disclaimer` 표시 |
| ETL (predict_logs) | `predicted_range_low/high_krw` 그대로 | 정상 (D 도 값 채움) |
| Grafana / monitoring | mdape / coverage | 새 `margin_policy_version` 으로 cohort split 가능 |

→ **외부 client 계약 깨짐 X**. v2 응답에서 `price_range` deprecate 는 별도 trk.

## 2. Server 변경

```python
# src/visionai/price_engine/api/primary_predictor.py

# 등급별 m table (artifact: model_test_results/grade_margin_calibration.json 참조)
GRADE_MARGIN_V1 = {"A": 0.20, "B": 0.30, "C": 0.50, "D": 0.70}  # legacy
GRADE_MARGIN_V2 = {"A": 0.286, "B": 0.609, "C": 0.50, "D": 0.70}  # 옵션 1
# C/D 는 m 변경 X (range_display_mode 로 frontend 처리)

def _get_margin(grade: str, policy_version: str) -> float:
    table = GRADE_MARGIN_V2 if policy_version == "v2" else GRADE_MARGIN_V1
    return table.get(grade, 0.7)
```

**등급별 분기 (server 응답 build)**:

```python
# src/visionai/price_engine/api/primary_server.py

def _build_prediction(result: dict, grade: str, policy: str) -> Prediction:
    margin = _get_margin(grade, policy)
    pr = PriceRange(
        low=int(result["price_krw"] * (1 - margin)),
        high=int(result["price_krw"] * (1 + margin)),
    )
    if grade in ("A", "B"):
        return Prediction(
            ...,
            price_range=pr,
            range_display_mode="range",
            range_visible=True,
            disclaimer=None,
            margin_policy_version=policy,
        )
    if grade == "C":
        return Prediction(
            ...,
            price_range=pr,  # 호환 — 값 채움
            range_display_mode="single",
            range_visible=False,
            disclaimer="정확도 낮음 — 단일 참고가만 사용 권장",
            margin_policy_version=policy,
        )
    # D
    return Prediction(
        ...,
        price_range=pr,  # 호환 — 값 채움
        range_display_mode="hidden",
        range_visible=False,
        disclaimer=None,
        margin_policy_version=policy,
    )
```

## 3. Logging schema 확장 (코덱스 빠진 작업)

`predict_logs` DDL 에 1 column 추가 (PR14a/b 의 alias 패턴 따름):

```sql
ALTER TABLE predict_logs ADD COLUMN margin_policy_version VARCHAR(8);
-- 'v1' | 'v2' (옵션 1) — cohort split 분석용
```

server `_log_prediction` 에 `margin_policy_version` 필드 추가.

## 4. 점진 Rollout (코덱스 필수 권고)

### 4.1 Offline replay (D-7 ~ D-3)

`scripts/v3_7_grade_margin_replay.py`:
- 학습 dataset 28,376 row 의 production 라우팅 결과 + 새 m 으로 가격 범위 재계산
- 등급별 새 coverage 측정 (목표: A 80% / B 80%)
- v1 vs v2 비교 표 산출 → 운영팀에 사전 공유

### 4.2 Shadow logging (D-3 ~ D-day)

production server 가 v1 (현재) 응답하되 logs 에 v1 + v2 둘 다 기록:
```python
log = {
    ...,
    "predicted_range_low_krw_v1": int(price * (1 - 0.20)),
    "predicted_range_high_krw_v1": int(price * (1 + 0.20)),
    "predicted_range_low_krw_v2": int(price * (1 - 0.286)),  # shadow
    "predicted_range_high_krw_v2": int(price * (1 + 0.286)),
    "margin_policy_version": "v1",  # 응답은 v1
}
```

→ production traffic 1주로 새 coverage 검증 가능 (A/B 등급 80% 목표 충족 확인).

### 4.3 Rollout 단계 (v3.6 cohort 패턴 활용)

```
shadow-only (D-3 ~ D-day, 응답 v1)
    ↓ offline + shadow coverage 검증
margin_policy_version=v2 5%   (D-day, rollout_cohort=treatment_5pct 만 v2)
    ↓ 24h
v2 25%                         (D+1)
    ↓ 24h
v2 100%                        (D+2)
```

### 4.4 판정 지표 (MdAPE 가 아님 — 코덱스 명시)

- `grade 별 range coverage` (A/B 80% 목표 / 현재 71.5% / 50.6%)
- `C/D 노출률` (`range_display_mode='single' or 'hidden'` 비율)
- `사용자 피드백` (frontend / customer-success 측 수집)

## 5. 작업 항목 (4d, 코덱스 권고 반영)

| # | 작업 | 위치 | 작업량 |
|---|------|------|------:|
| 1.1 | predictor `_get_margin` 추가 + GRADE_MARGIN_V1/V2 | `primary_predictor.py` | 0.3d |
| 1.2 | additive schema 3 field + Pydantic validator | `primary_schemas.py` | 0.3d |
| 1.3 | server 등급별 분기 + shadow logging | `primary_server.py` | 0.5d |
| 1.4 | predict_logs DDL alter + ETL alias | `monitoring/sql/` + `etl_predict_logs.py` | 0.3d |
| 1.5 | endpoint test (4 등급 × 응답 + shadow logging 정합) | tests/ | 0.6d |
| 1.6 | **consumer contract test** — frontend / batch / demo / api_reference 별 | tests/ | 0.5d |
| 1.7 | offline replay script + v1/v2 비교 표 | `scripts/v3_7_grade_margin_replay.py` | 0.7d |
| 1.8 | Grafana panel 13 (margin_policy_version 별 coverage) | `monitoring/grafana/` | 0.3d |
| 1.9 | api_reference.html + demo.html 갱신 | docs/ | 0.4d |

**총**: 약 **4.0일** + offline replay 결과 review + rollout 1주.

## 6. Frontend 협조 사항

신규 3 field 처리:
- `range_display_mode == "range"` → 기존 그대로 표시
- `range_display_mode == "single"` → `price_krw` 만 표시 + `disclaimer` 작은 글씨
- `range_display_mode == "hidden"` → `price_krw` 만 + 범위 hide
- `range_visible` boolean 도 동일 — display mode 의 빠른 hint

frontend 변경 PR 별도 (협력자 측). backend 는 신규 field 만 노출 — frontend 가 무시해도 backward compat.

## 7. Rollback plan

- v2 만 끄기: env `MARGIN_POLICY_VERSION=v1` 로 강제 (server side flag)
- 응답 schema 자체 rollback 은 X (additive only — 기존 field 그대로)
- shadow logging 만 켜진 상태로 1주 운영 → 문제 시 v2 rollout 자체 취소

## 관련 문서

- 결정 source: `docs/model_technical_report_v2.html` §6.2
- 협조 정리: `docs/협조_필요사항_정리_20260428.md` §3.1 (m 값 결정)
- 권장 m artifact: `model_test_results/grade_margin_calibration.json`
