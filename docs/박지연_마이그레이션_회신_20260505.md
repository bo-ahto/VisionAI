# 박지연님 회신 (마이그레이션 협의) — 2026-05-05

## 회신 권고 메시지 (Slack 용)

> @박지연 안녕하세요, V4 cycle 검증 완료해서 결과 공유 드립니다.
>
> ## Top 30 검수 + 모델 검증 결과
> Top 30 갤러리 검수 결과 잘 받아서 v4 갤러리 리스트로 통합했습니다 (총 118개 갤러리). 이후 11단계 검증 (coverage / ablation / Tier segment / holdout / hyperparameter tuning / fairness check / repeated 3-seed holdout) 진행했고, 다음과 같이 결정했습니다:
>
> **운영 도입 보류 + Saatchi 학습 v4 제외 default policy 채택**
>
> ### 근거 요약
> - 매칭률은 13% → 95%로 크게 향상 ✅
> - 단일 split 결과는 개선처럼 보였으나 (CB Artsy -2.95pp), 3-seed repeated holdout 에서 재현 안 됨 (mean +2.12 ± 4.42)
> - CatBoost 의 Saatchi 학습에서 일관된 악화 (3/3 seeds, +0.99 ± 0.88) → Saatchi v4 제외 정책 채택
> - 자세한 의사결정 메모: docs/v4_운영도입_의사결정_20260504.md (PR #26)
>
> ## 마이그레이션 일정
> 본 마이그레이션 데이터 도착 시 추가 미매칭 갤러리 일괄 검수 요청 드릴 예정입니다 (양식 미리 준비 완료). 일정 알려주시면 맞춰 준비하겠습니다.
>
> ## 중간 협조 요청
> 갤러리 판매 데이터 292건 샘플에서 **8개 신규 갤러리** 가 v4 리스트 미수록입니다:
> - 리만머핀 갤러리 (32건), 리슨 갤러리 (28건), 갤러리 르롱 (12건) — 글로벌 메이저 한국 진출
> - 워크스워크스 (34건), 제시카 실버맨 (30건), 갤러리 술타나 (21건), 더써드갤러리 (12건), 이아 갤러리 (8건) — 국내/해외 신규
>
> 본 마이그레이션 시 이들 + 추가 미매칭 갤러리 한꺼번에 검수 부탁드립니다 (Top 30 과 동일 양식).
>
> 또한 표기 차이로 alias 처리한 케이스 3건 별도 공유:
> - 타데우스 로팍 ↔ "타데우스 로팍 서울" (지점 표기)
> - 야리라거 갤러리 ↔ "야리 라거" (띄어쓰기)
> - 갤러리기체 ↔ "갤러리 기체" (띄어쓰기)
>
> 이상 있으시면 알려주세요. 감사합니다.

## 핵심 톤 가이드라인

- **부정적 결과를 솔직히 공유** — "도입 보류" 명확히 (사후 신뢰)
- **검증 process 강조** — 11단계 + 코덱스 5차 자문 (qualitative 신뢰)
- **데이터 가치는 보존** — v4 리스트 자체는 운영 reference 로 유지
- **마이그레이션 일정은 협력자 페이스로** — 압박 X, 준비됨 표시
- **추가 검수 미리 예고** — 신규 미매칭 8개 + α

## 이미 준비된 자료 (필요 시 공유)

- `docs/v4_운영도입_의사결정_20260504.md` — 의사결정 메모 전문 (technical detail)
- `model_test_results/repeated_holdout_check.json` — 검증 metric raw data
- `model_test_results/v4_full_verification_report.md` — 검증 종합 리포트

## 본 마이그레이션 시 추가 검수 요청서 양식

다음 마이그레이션 데이터 도착 시 **사용 가능한 template** — V4 의 검수요청서 (`docs/협조_응답_미매칭갤러리_검수요청_20260503.md`) 형식 재사용:

### Template Skeleton

```markdown
# 미매칭 갤러리 추가 검수 요청 ([Migration Round 1])

> **작성일**: YYYY-MM-DD
> **요청 출처**: v5 cycle 시작 전 — 본 마이그레이션 데이터 통합 시 신규 미매칭
> **수신**: 박지연
> **소요 예상**: 협력자 1-2일 (갤러리 N개)

## 0. 요청 요약

본 마이그레이션 데이터 N건 통합 결과, v4 갤러리 리스트 (118건) 와 미매칭인 갤러리 M개 발견. 이들의 한글명 / 티어 / 분류 검수 부탁드립니다.

V4 cycle 의 Top 30 검수와 동일 양식 사용.

## 1. 데이터 source

- 본 마이그레이션 데이터 (1차 시장 + 갤러리 판매)
- 분석 기준: `analyze_gallery_tier_coverage.py` (v4 리스트 + alias map)
- 신규 미매칭 갤러리 M개 (작품 K건)

## 2. 미매칭 갤러리 명단 (CSV)

`docs/unmatched_galleries_round_N.csv` 첨부.

| 순위 | 영문명 | 한글명 (있으면) | 작품 수 | 본 데이터 점유 % |
|---|---|---|---|---|
| 1 | Lehmann Maupin | 리만머핀 갤러리 | 32 | x.xx% |
| ... | ... | ... | ... | ... |

## 3. 검수 요청 항목 (Top 30 양식 재사용)

`unmatched_galleries_round_N.csv` 의 4개 컬럼 채워서 회신:
- `명칭_한글_확정` (필수)
- `티어` (Tier A-E 5택)
- `분류` (갤러리 / 기관 / 기타 — 3택)
- `리스트_추가_여부` (yes / no)
- `비고` (선택)

[V4 검수요청서의 컬럼별 허용 값 표 그대로 재사용]

## 4. 후속 작업 (운영 측)

회신 받은 후:
1. v4 → v5 갤러리 리스트 갱신 (`art_gallery_tier_list_v5.csv`)
2. alias map 업데이트 (필요 시 표기 정규화 추가)
3. V5 cycle 시작 (사전등록 finalize + Day 1)

## 5. V4 cycle 학습 반영

- v4 운영 도입 보류 결정 (검증 결과 재현성 부족) → V5 cycle 사전등록 protocol 도입
- 본 데이터 검수 결과는 v5 cycle confirmatory 학습 입력
```

## V5 cycle 시작 직전 체크리스트 (마이그레이션 후)

본 마이그레이션 데이터 도착 시 즉시 진행 가능한 작업 순서:

- [ ] 데이터 통합 + analyze_gallery_tier_coverage.py 실행 → 미매칭 갤러리 명단 추출
- [ ] 미매칭 갤러리 검수 요청서 작성 (위 template) + 박지연님 발송
- [ ] V5 사전등록 분포-의존 항목 확정 (X cell drop threshold, segment 최소 n)
- [ ] 사전등록 본 commit (`docs/v5_cycle_사전등록.md`)
- [ ] LAO split 재생성 + DINOv2 embedding 재계산
- [ ] V5 Day 1 시작 (compressed re-check + R PoC 본 검증)

## 관련 PR / 자료

- PR #26 (V4/V5 통합) — feature/gallery-tier-v4-research → docs/technical-report-v2
- V5 cycle plan: `docs/v5_cycle_plan_20260504.md`
- V5 사전등록 초안 (PILOT-driven 수정): `docs/v5_cycle_사전등록_초안.md`
- R composite design + PoC 결과: `docs/v5_R_composite_design.md`
