# Track 3 실험 코드 재현용 의존성 (PR15/17/18, production_train)
# 작성일 2026-05-12 / Python 3.14.2 기준

pandas>=2.0,<4.0
numpy>=1.26,<3.0
scikit-learn>=1.4,<2.0
scipy>=1.11,<2.0
lightgbm>=4.0,<5.0
joblib>=1.3,<2.0
pyarrow>=14.0   # parquet 읽기 (unified v3 사용 시)

# 작성 환경 (참고용 — 동일 결과 재현 시):
#   python=3.14.2
#   pandas=3.0.1
#   numpy=2.4.3
#   scikit-learn=1.8.0
#   scipy=1.17.1
#   lightgbm=4.6.0
#   joblib=1.5.3
#   platform=macOS-26.4.1-arm64

# 사용:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   python3 track3_depth_ablation.py   # PR15 ablation 실행
